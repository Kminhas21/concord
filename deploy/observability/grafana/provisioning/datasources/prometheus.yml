# Grafana datasource provisioning: point Grafana at Prometheus automatically on
# boot, with a fixed uid the dashboard JSON references. No manual setup.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
