# Normalize line endings to LF in the repo so Windows checkouts and Linux CI
# produce identical bytes (keeps `buf generate` + `git diff --exit-code` honest).
* text=auto eol=lf
*.go text eol=lf
*.proto text eol=lf
*.md text eol=lf
